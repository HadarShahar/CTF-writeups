aesebootiv
eimahquuof
nieteidiel // redirect the touch command: echo '/bin/sh' > touch; chmod +x touch; PATH=/tmp/jim:$PATH /behemoth/behemoth2
ietheishei // overwrite puts@got
aizeeshing // symbolic link to the password
mayiroeche // open udp connection with nc (linux ignores the setuid bit when running a process under strace or similar programs!)
baquoxuafo // write shellcode to shellcode.txt file, and in the shell that is opened write: "echo HelloKitty" and then exit the shell
pheewij7Ae // simple buffer overflow with large padding in the beginning

ssh behemoth8@behemoth.labs.overthewire.org -p 2221



// download a file:
scp -P 2221 behemoth7@behemoth.labs.overthewire.org:/behemoth/behemoth7 .

